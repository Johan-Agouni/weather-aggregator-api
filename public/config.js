/**
 * AtmoSphere — Frontend Configuration
 *
 * After deploying the API on Railway, replace the URL below with your Railway domain.
 * In local dev, leave empty (same-origin).
 *
 * Example: const API_BASE_URL = 'https://atmosphere-api-production.up.railway.app';
 */
const API_BASE_URL = window.ATMOSPHERE_API_URL || 'https://atmosphere-api-production.up.railway.app';
